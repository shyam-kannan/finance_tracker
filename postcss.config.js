// PostCSS configuration file that defines which plugins to use for CSS processing
export default {
  plugins: {
    // Tailwind CSS plugin - processes Tailwind utility classes and generates CSS
    tailwindcss: {},
    // Autoprefixer plugin - automatically adds vendor prefixes to CSS properties for cross-browser compatibility
    autoprefixer: {},
  },
};